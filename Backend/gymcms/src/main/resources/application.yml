server:
  port: 8080

spring:
  datasource:
    url: jdbc:postgresql://localhost:5442/gym_cms_db
    username: damcio
    password: password
  jpa:
    hibernate:
      ddl-auto: create-drop
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  security:
    user:
      name: admin
      password: admin
